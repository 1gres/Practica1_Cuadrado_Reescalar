#version 330 core

//in vec3 ourColor;
in vec2 TexCoord;

// Ouput data
out vec4 color;

//Creem les 2 textures
uniform sampler2D Texture1;

void main()
{
	color = texture(Texture1, TexCoord);
}